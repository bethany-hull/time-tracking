{
  "name": "time-tracking-backend",
  "version": "1.0.0",
  "description": "Cloud Run backend for time tracking app - proxies Gemini API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "deploy": "gcloud run deploy time-tracking-api --source . --region us-central1 --allow-unauthenticated"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "cors": "^2.8.5",
    "express": "^4.21.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
